<UserControl
    x:Class="OsuPlayer.Views.Pages.RecentPlayPage"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:lang="clr-namespace:OsuPlayer.Lang"
    xmlns:core="clr-namespace:OsuPlayer.Core;assembly=OsuPlayer.Core"
    xmlns:vm="clr-namespace:OsuPlayer.ViewModels"
    x:DataType="vm:RecentPlayPageViewModel">
    <UserControl.Styles>
        <Style Selector="ListBox.recent-list > ListBoxItem">
            <Setter Property="Padding" Value="0" />
            <Setter Property="Margin" Value="0,0,0,1" />
        </Style>
    </UserControl.Styles>
    <Grid RowDefinitions="Auto,*">
        <StackPanel
            Grid.Row="0"
            Margin="10"
            Orientation="Horizontal"
            Spacing="8">
            <Button
                Classes="play-button"
                Command="{Binding PlayAllCommand}"
                Content="{I18N {x:Static lang:SRKeys.Ui_Btn_PlayAll}}" />
            <Button
                Classes="settings-button"
                Command="{Binding ClearAllRecentCommand}"
                Content="{I18N {x:Static lang:SRKeys.Ui_Btn_DeleteAll}}" />
        </StackPanel>
        <Grid Grid.Row="1" Margin="5,0,5,5" RowDefinitions="Auto,*">
            <Border Grid.Row="0" Padding="12,8" Background="#F4F5F7" BorderBrush="#E4E6EB" BorderThickness="0,0,0,1" VerticalAlignment="Top">
                <Grid ColumnDefinitions="40,3*,2*,*,2*">
                    <TextBlock FontWeight="SemiBold" Foreground="#666666" Text="#" />
                    <TextBlock Grid.Column="1" FontWeight="SemiBold" Foreground="#666666" Text="{I18N {x:Static lang:SRKeys.Ui_Title}}" />
                    <TextBlock Grid.Column="2" FontWeight="SemiBold" Foreground="#666666" Text="{I18N {x:Static lang:SRKeys.Ui_Artist}}" />
                    <TextBlock Grid.Column="3" FontWeight="SemiBold" Foreground="#666666" Text="{I18N {x:Static lang:SRKeys.Ui_Creator}}" />
                    <TextBlock Grid.Column="4" FontWeight="SemiBold" Foreground="#666666" Text="{I18N {x:Static lang:SRKeys.Ui_MapDifficulty}}" />
                </Grid>
            </Border>
            <ListBox
                x:Name="RecentList"
                Grid.Row="1"
                Classes="recent-list"
                Background="Transparent"
                DoubleTapped="RecentList_DoubleTapped"
                ItemsSource="{Binding Beatmaps}">
                <ListBox.ItemTemplate>
                    <DataTemplate x:DataType="core:BeatmapDataModel">
                        <Border Padding="12,10" Background="#FBFBFB" BorderBrush="#E6E6E6" BorderThickness="0,0,0,1">
                            <Grid ColumnDefinitions="40,3*,2*,*,2*">
                                <TextBlock Grid.Column="0" Foreground="#9C9C9C" Text="{Binding Index}" />
                                <TextBlock Grid.Column="1" Foreground="#222222" Text="{Binding AutoTitle}" />
                                <TextBlock Grid.Column="2" Foreground="#9C9C9C" Text="{Binding AutoArtist}" />
                                <TextBlock Grid.Column="3" Foreground="#222222" Text="{Binding AutoCreator}" />
                                <TextBlock Grid.Column="4" Foreground="#9C9C9C" Text="{Binding AutoVersion}" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel CacheLength="2" />
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
            </ListBox>
        </Grid>
    </Grid>
</UserControl>
